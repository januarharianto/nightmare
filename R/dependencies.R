# Central library loading for NIGHTMARE

load_nightmare_dependencies <- function() {
  library(shiny)
  library(bslib)
  library(dplyr)
  library(readr)
  library(readxl)
  library(stringr)
  library(shinyjs)
  library(jsonlite)
}
